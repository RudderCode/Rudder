interface:
  display_name: "Rudder"
  short_description: "Resolve a local spec, then generate tests"
  default_prompt: "Use $rudder to resolve the device-local spec, then generate and verify tests for this branch."
